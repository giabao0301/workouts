import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {
    color: '#2312B6',
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 10,
    marginBottom: 5,
  },
});

export default styles;
